    spring:
        cloud:
            gateway:
                globalcors:
                    corsConfigurations:
                        '[/**]':
                            allowedOrigins:
                                - "http://localhost:5555"
                            allowedMethods:
                                - GET
                                - POST
                                - PUT
                            allowedHeaders: "*"
#                                 - Content-Type
#                                 - Authorization
                routes:
                    - id: study-center:8190
                      uri: http://study-center
                      predicates: 
                        - Path=/study-center/**
#                         - Header=Authorization, Basic password
                    - id: payment-service:8189
                      uri: http://payment-service
                      predicates:
                        - Path=/payment-service/**
    eureka:
        client:
            registerWithEureka: true
            fetchRegistry: true
            serviceUrl.defaultZone: ${EUREKA_URI:http://localhost:8761/eureka} 
