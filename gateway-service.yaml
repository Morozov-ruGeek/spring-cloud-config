    spring:
        cloud:
            gateway:
                globalcors:
                    corsConfigurations:
                        '[/**]':
                            allowedOrigins:
                                - "http://localhost:8080"
                            allowedMethods:
                                - GET
                                - POST
                                - PUT
                            allowedHeaders: "*"
#                                 - Content-Type
#                                 - Authorization
                routes:
                    - id: study-center
                      uri: "ttp://study-center:8080"
                      predicates: 
                        - Path=/study-center/**
#                         - Header=Authorization, Basic password
                    - id: payment-service
                      uri: http://payment-service
                      predicates:
                        - Path=/payment-service/**
    eureka:
        client:
            registerWithEureka: true
            fetchRegistry: true
            serviceUrl.defaultZone: ${EUREKA_URI:http://localhost:8761/eureka} 
